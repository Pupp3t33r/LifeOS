# features/money/data/api

Generated Dio client from Money's `/openapi/v1.json` (OpenAPI Generator,
`dart-dio` template). All calls go through the Gateway at `/api/money/*` —
never directly to a service. Generated in Phase 5 once Money exposes the spec.
