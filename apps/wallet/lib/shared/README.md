# shared

Cross-feature widgets and utilities (e.g. Money formatting, the "original +
converted inline" currency renderer). No feature-specific logic here; features
do not reach into each other — they share only through this folder or the shell.
