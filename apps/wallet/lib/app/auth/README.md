# app/auth

Shell-level authentication. Keycloak OIDC via `flutter_appauth`; tokens stored
in `flutter_secure_storage`. Authority `…/realms/lifeos`, audience `money-api`
(see services/money ADR-0004). Built when API work begins (backend-gated).
